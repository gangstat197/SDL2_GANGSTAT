#ifndef MESSAGES_H
#define MESSAGES_H

#include <SDL.h>
#include <string>

// Logs an error message and exits the application
void LogErrorAndExit(const char* msg, const char* error);

#endif // MESSAGES_H
